import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'large-title-repro',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
